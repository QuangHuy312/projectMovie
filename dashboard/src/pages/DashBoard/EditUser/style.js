import styled from "styled-components";
import { Content } from "../UserManager/style";

export const ContentForm = styled(Content)`
  p {
    color: red;
    padding-left: 20px;
  }
  .form-item {
    margin-bottom: 0;
    margin-top: 20px;
  }
`;
